#heltec esp32 v2 w wifi, LoRa
#blinkerz with micro python
import time
from machine import Pin
led = Pin(2, Pin.OUT) #create LED object on Pin2
while True:
  led.value(1) # Set LED turn on
  time.sleep(0.5)
  led.value(0) # turn  off
  time.sleep(0.5)
